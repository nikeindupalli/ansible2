---
- hosts: all
  become: yes
  tasks:
  - name: install webserver on redhat
    yum:
     name: httpd
     state: present
    when: ansible_os_family= "Redhat"
  - name: start webserver on redhat
    service:
     name: httpd
     state: started
    when: ansible_os_family= "Redhat"